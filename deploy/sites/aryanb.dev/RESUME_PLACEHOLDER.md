The "Download Resume" button on this site links to `/resume.pdf`, which does not exist in
this repo yet. Drop your actual resume PDF at `deploy/sites/aryanb.dev/resume.pdf` (same
directory as `index.html`) before or after the first deploy — `publish.sh` copies everything
in this directory as-is, so no other change is needed once the file is in place. Until then
the button 404s; this file is a deliberate placeholder note, not a claim that the button
works today.
