// Installed to /etc/apt/apt.conf.d/50unattended-upgrades by deploy/bootstrap-server.sh.
// Security updates only — this box runs a public demo, not something to silently reboot.
Unattended-Upgrade::Allowed-Origins {
    "${distro_id}:${distro_codename}-security";
    "${distro_id}ESMApps:${distro_codename}-apps-security";
    "${distro_id}ESM:${distro_codename}-infra-security";
};

Unattended-Upgrade::Remove-Unused-Kernel-Packages "true";
Unattended-Upgrade::Remove-Unused-Dependencies "true";

// Deliberately false: an unattended reboot would kill the running app/DB containers with no
// warning. Ubuntu's motd/needrestart will flag a pending reboot for you to run by hand.
Unattended-Upgrade::Automatic-Reboot "false";

Unattended-Upgrade::SyslogEnable "true";
