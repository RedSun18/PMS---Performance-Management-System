@page
@model PerformanceManagement.Web.Pages.Account.LoginModel
@{
    ViewData["Title"] = "Sign in";
    Layout = "_Layout";
}
<div class="login-box card">
    <div class="card-header">Sign in — Performance Management System</div>
    <div class="card-body">
        @if (Model.Error is not null)
        {
            <div class="alert alert-error">@Model.Error</div>
        }
        <form method="post" data-no-dirty-check>
            <input type="hidden" name="ReturnUrl" value="@Model.ReturnUrl" />
            <div style="margin-bottom:12px;">
                <label for="username">Username</label>
                <input type="text" id="username" name="username" value="@Model.Username" autofocus />
            </div>
            <div style="margin-bottom:16px;">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" />
            </div>
            <div style="margin-bottom:16px;">
                <label style="text-transform:none;"><input type="checkbox" id="rememberMe" name="rememberMe" value="true" style="width:auto;" /> Remember me on this device</label>
            </div>
            <button type="submit" class="btn btn-primary" style="width:100%;">Sign in</button>
        </form>
        <p class="muted" style="margin-top:14px;">
            Development accounts: <code>admin</code> (administrator) and one account per
            employee (4-digit employee code). See the README for initial passwords.
        </p>
    </div>
</div>
